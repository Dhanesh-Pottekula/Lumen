{
  "name": "aira_canvas",
  "private": true,
  "version": "1.0.0",
  "description": "AiRA tutorial-slide animation prototypes — canvas renderFrame(t) vs SVG/CSS declarative, as separate React components.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "extract-narration": "tsx scripts/extract-narration.ts",
    "generate-narration": "tsx scripts/generate-narration.ts"
  },
  "dependencies": {
    "@cartesia/cartesia-js": "^3.2.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
